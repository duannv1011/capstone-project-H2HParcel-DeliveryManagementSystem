import { Updatewarehoursedto } from './updatewarehoursedto';

describe('Updatewarehoursedto', () => {
    it('should be defined', () => {
        expect(new Updatewarehoursedto()).toBeDefined();
    });
});
